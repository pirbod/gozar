apiVersion: v1
kind: ConfigMap
metadata:
  name: gozar-siem-detection-rules
  namespace: gozar-controlled-demo
data:
  rules.md: |
    Local detection rules are stored under security/detection/rules/.
    Rules are evaluated by scripts/security/run_detection_tests.py without external SIEM calls.
