apiVersion: v1
kind: ServiceAccount
metadata:
  name: gozar-gateway
  namespace: gozar-production
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gozar-gateway
  namespace: gozar-production
  labels:
    app.kubernetes.io/name: gozar-gateway
    app.kubernetes.io/part-of: gozar
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: gozar-gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gozar-gateway
        app.kubernetes.io/part-of: gozar
    spec:
      serviceAccountName: gozar-gateway
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: gateway
          image: ghcr.io/pirbod/gozar-gateway:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: quic
              containerPort: 6200
              protocol: UDP
          env:
            - name: RUST_LOG
              value: info
            - name: GOZAR_NODE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: GOZAR_ROLE
              value: gateway
            - name: GOZAR_CONTROL_PLANE_URL
              value: http://gozar-control-plane:8080
            - name: GOZAR_LISTEN_ADDR
              value: 0.0.0.0:6200
            - name: GOZAR_QUEUE_LIMIT
              value: "64"
            - name: GOZAR_CONTROL_SECRET
              valueFrom:
                secretKeyRef:
                  name: gozar-node-secrets
                  key: control-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: gozar-gateway
  namespace: gozar-production
  labels:
    app.kubernetes.io/name: gozar-gateway
    app.kubernetes.io/part-of: gozar
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gozar-gateway
  ports:
    - name: quic
      port: 6200
      targetPort: quic
      protocol: UDP
