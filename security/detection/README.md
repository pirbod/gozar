# SIEM-Style Detection Logic

Rules in `rules/` are local YAML detections for controlled demo events. Sample redacted events live in `sample-events/`. The evaluator does not call external systems.

Run:

```bash
python scripts/security/run_detection_tests.py
```
